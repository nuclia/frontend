import { ResourcesTableDirective } from './resources-table.directive';

describe('ResourcesTableDirective', () => {
  it('should create an instance', () => {
    const directive = new ResourcesTableDirective();
    expect(directive).toBeTruthy();
  });
});
