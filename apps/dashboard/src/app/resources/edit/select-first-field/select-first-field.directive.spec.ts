import { SelectFirstFieldDirective } from './select-first-field.directive';

describe('SelectFirstFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectFirstFieldDirective();
    expect(directive).toBeTruthy();
  });
});
